# Inspector END example agent dependencies.
# MonoRAG is installed from the parent repo (pip install -e ../..)
# or from GitHub (pip install git+https://github.com/BryanInfante/monorag.git)

fastapi==0.115.0
uvicorn==0.30.6
python-dotenv==1.2.2
